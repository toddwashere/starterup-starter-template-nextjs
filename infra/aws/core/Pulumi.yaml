name: starter-aws-core
runtime:
  name: nodejs
  options:
    # Native ESM ("type": "module") + relative TS imports need a TS loader.
    typescript: false
    nodeargs: "--import tsx"
description: Core AWS resources for the SaaS starter template (RDS, SQS, secrets)
