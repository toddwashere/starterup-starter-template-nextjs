name: starter-aws-apps
runtime:
  name: nodejs
  options:
    # Native ESM ("type": "module") + relative TS imports need a TS loader.
    typescript: false
    nodeargs: "--import tsx"
description: App Runner services and workers Lambda for the SaaS starter template
