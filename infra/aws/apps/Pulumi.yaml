name: starter-aws-apps
runtime:
  name: nodejs
  options:
    # Native ESM ("type": "module") + relative TS imports need a TS loader.
    typescript: false
    nodeargs: "--import tsx"
description: ECS Fargate services for the SaaS starter template
config:
  aws:region:
    description: AWS region
